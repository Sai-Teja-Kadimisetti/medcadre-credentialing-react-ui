import React from 'react';

const ValidationFieldsConfig: React.FC = () => {
  return (
    <div>
      <h1>ValidationFieldsConfig Document Type</h1>
      <p>Configure settings for ValidationFieldsConfig document types.</p>
    </div>
  );
};

export default ValidationFieldsConfig;
