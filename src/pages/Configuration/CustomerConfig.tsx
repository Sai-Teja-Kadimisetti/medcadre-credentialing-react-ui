import React from 'react';

const CustomerConfig: React.FC = () => {
  return (
    <div>
      <h1>CustomerConfig Document Type</h1>
      <p>Configure settings for CustomerConfig document types.</p>
    </div>
  );
};

export default CustomerConfig;
