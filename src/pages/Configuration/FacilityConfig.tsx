import React from 'react';

const FacilityConfig: React.FC = () => {
  return (
    <div>
      <h1>FacilityConfig Document Type</h1>
      <p>Configure settings for FacilityConfig document types.</p>
    </div>
  );
};

export default FacilityConfig;
